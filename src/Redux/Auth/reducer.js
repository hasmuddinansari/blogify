
const intialState = {
    isLoggedIn: false,
    curr_user: "",
    all_user_list: [],
    error: ""
}

const reducer = (state = intialState, action) => {
    switch (action.type) {
        default: return state
    }
}

export default reducer